overflow속성은 일반적으로 auto, hidden값을 사용한다. 
특히나 overflow:hidden은
position, float 속성으로 어떤 객체를 하늘로 띄웠을때 부모요소가 그 하늘로 뜬 객체를 인지하는 기능으로 사용된다
결국 자식요소를 확인하는 기능이다.
-float
p는 '문단'
선택자중 ">"
부모 > 자식 : 직계자식만 선택하고자 할 때 사용

css에서 삼각형 만들기
- box의 border값을 활용해서 삼각형 구조를 만들 수 있다
- 이때 border-color 속성을 이용한다
- 값은 원하는 color값과 transparent(투명값)값을 주면서 모양을 만든다.

